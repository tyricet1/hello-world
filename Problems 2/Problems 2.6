#include <iostream>
#include <iomanip>
using namespace std;

int main()
{
	// variables
	double cupsOfSugar = 0.03125;
	double cupsOfButter = 0.02083;
	double cupOfFlower = 0.05729;
	double cookies;
	double recipe1;
	double recipe2;
	double recipe3;
	double ingredients;
	//get data

	cout << "Enter the amout of cookies you want to make: ";
	cin >> cookies;
	
	//Calculations
	recipe1 = cookies * cupsOfSugar;
	recipe2 = cookies * cupsOfButter;
	recipe3 = cookies * cupOfFlower;

	//output

	cout << "The ingredients needed for this is: " << endl; 
	cout << recipe1 << setw(5) << setprecision(2) << fixed << " cups of sugar." << endl; 
	cout << recipe2 << setw(5) << setprecision(2) << fixed << " cups of butter." << endl;
	cout << recipe3 << setw(5) << setprecision(2) << fixed << " cups of flower." << endl;

	return 0;
}
